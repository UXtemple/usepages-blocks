import { PropTypes } from 'react'

const List = () => null

List.propTypes = {
  from: PropTypes.string,
  of: PropTypes.object,
  item: PropTypes.string,
  variable: PropTypes.string
}

export default List
